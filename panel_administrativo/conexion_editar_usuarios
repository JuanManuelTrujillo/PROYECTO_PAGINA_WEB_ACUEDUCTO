<?php

include 'conexion_registro.php';

    $nombre = $_POST['nombre'];
    $correo = $_POST['correo'];
    $usuario = $_POST['usuario'];
 
    $sql = "UPDATE registro_usuarios SET 
                    nombre='".$nombre."',
                    correo='".$correo."',
                    usuario='".$usuario."'";

    if ($resultado = $conn->query($sql)) {
        header("location: registro_usuarios.php");
    }
